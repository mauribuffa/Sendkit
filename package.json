{
  "name": "sendkit-workspace",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "dev:cli": "bun run packages/cli/src/index.ts",
    "dev:local-mcp": "bun run packages/local-mcp/src/index.ts",
    "dev:remote-mcp": "bun run apps/remote-mcp/src/index.ts",
    "format": "oxfmt . --write",
    "format:check": "oxfmt . --check",
    "lint": "oxlint . --deny-warnings",
    "lint:fix": "oxlint . --fix",
    "build:core": "bun run --filter @mrb-dev/sendkit-core build",
    "build:cli": "bun run --filter @mrb-dev/sendkit build",
    "build:local-mcp": "bun run --filter @mrb-dev/sendkit-mcp build",
    "release:pack:core": "bun run --filter @mrb-dev/sendkit-core pack:dry",
    "release:pack:cli": "bun run --filter @mrb-dev/sendkit pack:dry",
    "release:pack:local-mcp": "bun run --filter @mrb-dev/sendkit-mcp pack:dry",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "oxfmt": "^0.57.0",
    "oxlint": "^1.72.0",
    "tsdown": "^0.22.3",
    "typescript": "^6.0.3"
  }
}
